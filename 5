import numpy as np
from sklearn.neural_network import MLPClassifier

# Carregando dados de treino
df_treino = pd.read_excel('dados Trafos.xlsx', sheet_name='tab_treinamento')
X_treino = df_treino[['X1', 'X2', 'X3']]
y_treino = df_treino['d']

# Função para executar e extrair dados de um treinamento
def treinar_e_extrair(random_state):
    # Inicializando com pesos aleatórios diferentes via random_state
    mlp = MLPClassifier(hidden_layer_sizes=(4,), activation='tanh', solver='sgd', 
                        learning_rate_init=0.01, max_iter=2000, random_state=random_state)
    mlp.fit(X_treino, y_treino)
    return mlp

mlp_T1 = treinar_e_extrair(random_state=10) # Treinamento 1
mlp_T2 = treinar_e_extrair(random_state=42) # Treinamento 2

print(f"T1 - Épocas: {mlp_T1.n_iter_}")
print(f"T2 - Épocas: {mlp_T2.n_iter_}")
# Para obter a tabela de pesos e bias, acesse mlp_T1.coefs_ e mlp_T1.intercepts_
